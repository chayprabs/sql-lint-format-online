## Summary

Describe the SQLGuard change and which PRD areas it touches (lint, format, rewrite, preflight, UI).

## Checklist

- [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, and `pnpm build` pass locally
- [ ] E2E updated if UI workflows changed
- [ ] Browser-only: no server upload of user SQL
