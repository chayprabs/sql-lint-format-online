{
  "name": "@sqlguard/cli",
  "version": "1.0.0",
  "description": "CLI wrapper for SQLGuard lint and format",
  "type": "module",
  "license": "MIT",
  "bin": {
    "sqlguard": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format esm --clean",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@sqlguard/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "tsup": "^8.5.0",
    "typescript": "^6.0.3"
  }
}
